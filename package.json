{
  "name": "ingilizce-tahta",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run generate:rehber && vite build",
    "preview": "vite preview",
    "generate:rehber": "node scripts/generate-rehber.mjs",
    "sync:remote": "node scripts/sync-remote-config.mjs"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "vite-plugin-pwa": "^1.3.0"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
